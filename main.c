#include <stdio.h>
/* trying to follow betty style */

/**
 * main - Entry point
 *
 * Return: return
 */

	int main(void)
{
	int n;
	 printf("size of type 'char' on my computer: %lu
	bytes\n", sizeof(char));
	 printf("size of type of my variable on my compu	ter: %lu bytes\n", sizeof(n));

	 return (0);
}
